------------------- rename command examples ---------------------------

rename "s/\-/\./g" *

			To replace all dashes(-) with dots(.) or use:

rename -n "s/\-/\./g" *

			First to do a test run before the actual replacement.

rename -v "s/\-/\./g" *

			Rename File with vervose mode.

for further Check 

man rename

